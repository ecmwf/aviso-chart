apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aviso-server.fullname" . }}-config
  labels:
    {{- include "aviso-server.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
